def linked_list_from_string(s):
    if s == 'None':
        return None
    s_sp = s.split(' -> ')[:-1]
    s_ints = [int(i) for i in s_sp]
    prev = None
    for i in s_ints[::-1]:
        node = Node(i, prev)
        prev = node
    return prev
