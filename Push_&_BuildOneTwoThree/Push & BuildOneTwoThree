def push(head, data):
    n_node = Node(data)
    n_node.next = head
    return n_node

def build_one_two_three():
    head = None
    head = push(head, 3)
    head = push(head, 2)
    head = push(head, 1)
    return head
